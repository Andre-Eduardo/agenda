{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "${configDir}/dist",
    "noEmit": false
  },
  "exclude": [
    "${configDir}/node_modules",
    "${configDir}/dist",
    "${configDir}/test",
    "${configDir}/*.config.ts",
    "${configDir}/src/**/*.test.ts"
  ]
}
