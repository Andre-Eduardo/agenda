/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Automo API
 * The backend API for the Automo project<h3>Authentication</h3>
                For the protected routes, you must first <b>sign up</b> (<code>/user/sign-up</code>) and then <b>sign in</b> (<code>/auth/sign-in</code>).
                After that, you already can request the protected routes.
                The API will return a token that will be included automatically in the cookie and will be sent in subsequent requests for authentication.
                To sign out, simply make a request to the <code>/auth/sign-out</code> endpoint,
                the token will be invalidated and removed from the cookie.
 * OpenAPI spec version: 0.0.0
 */
import type { MemberBillingReportPlanCode } from './memberBillingReportPlanCode';
import type { ModelBreakdownEntry } from './modelBreakdownEntry';

export interface MemberBillingReport {
  memberId: string;
  displayName: string;
  planCode: MemberBillingReportPlanCode;
  planPriceBrl: number;
  periodYear: number;
  periodMonth: number;
  /** Docs uploaded in the period */
  docsCount: number;
  /** Chat messages (usage record counter) */
  chatMessages: number;
  /** Clinical images in the period */
  imagesCount: number;
  /** Total AI cost in USD */
  totalAiCostUsd: number;
  /** Total AI cost in BRL */
  totalAiCostBrl: number;
  /** Number of completed chat interactions with AI */
  aiInteractionCount: number;
  /** Average cost per message in USD */
  avgCostPerMessageUsd: number;
  modelBreakdown: ModelBreakdownEntry[];
  /** Monthly plan revenue in BRL */
  planRevenueBrl: number;
  /** Total cost in BRL (AI + future add-ons) */
  totalCostBrl: number;
  /** Gross margin in BRL */
  grossMarginBrl: number;
  /** Gross margin percent */
  grossMarginPercent: number;
}
