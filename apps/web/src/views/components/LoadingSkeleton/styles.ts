import {css} from '@/styled-system/css';

export const root = css({
    display: 'flex',
    flexDirection: 'column',
    gap: '2',
});

export const row = css({
    w: 'full',
    rounded: 'card-sm',
});
