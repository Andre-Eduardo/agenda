import {css} from '@/styled-system/css';

export const memberCell = css({display: 'flex', alignItems: 'center', gap: '3'});
export const memberName = css({fontSize: 'sm', color: 'text.primary'});
export const skeletonRow = css({h: '14', mb: '2', rounded: 'card'});
export const mutedText = css({fontSize: 'sm', color: 'text.tertiary'});
export const actionCell = css({display: 'flex', justifyContent: 'flex-end'});
