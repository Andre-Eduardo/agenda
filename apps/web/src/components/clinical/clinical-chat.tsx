/**
 * RAG-enabled chat with patient context. AI replies wrap in <AIBlock>.
 * Per design-system.md §10.4, the frontend NEVER offers agent selection —
 * the backend resolves the agent from the professional's profile. The UI
 * may show the resolved agent name informationally only.
 *
 * TODO: implement when the clinical-chat page is built.
 */
export function ClinicalChat() {
  return null;
}
