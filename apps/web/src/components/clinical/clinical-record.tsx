/**
 * Structured SOAP record entry (Subjective, Objective, Assessment, Plan).
 * See design-system.md §13.
 *
 * TODO: implement when the record/new and record/edit pages are built.
 * AI-generated drafts should be wrapped in <AIBlock> until reviewed.
 */
export function ClinicalRecord() {
  return null;
}
