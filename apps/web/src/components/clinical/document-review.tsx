/**
 * Editable OCR review surface — extracted fields with confidence scores.
 * Pair each field with <ConfidenceIndicator>. See design-system.md §10.3 / §13.
 *
 * TODO: implement when document upload + review page is built.
 */
export function DocumentReview() {
  return null;
}
