/**
 * Per-specialty form rendered from a JSON schema (form-template).
 * Uses React Hook Form + Zod under the hood — see docs/frontend/08-forms.md.
 *
 * TODO: implement when the patient-form/fill page is built.
 */
export function DynamicForm() {
  return null;
}
