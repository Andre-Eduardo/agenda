/**
 * Appointment calendar built on react-day-picker v9 with Tailwind tokens.
 * See design-system.md §13. Note: this component lives under `agenda/`
 * (not `clinical/`) per the design-system component map.
 *
 * TODO: implement when the agenda/appointments page is built.
 */
export function AgendaCalendar() {
  return null;
}
