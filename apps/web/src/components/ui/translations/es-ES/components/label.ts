import type {Dictionary} from '../..';
import type {LabelTranslations} from '../../../components/Label/translations';

export const label: Dictionary<LabelTranslations> = {
    optional: '(opcional)',
};
