import type {Dictionary} from '../..';
import type {NumberInputTranslations} from '../../../components/NumberInput/translations';

export const numberInput: Dictionary<NumberInputTranslations> = {
    increment: 'Increment',
    decrement: 'Decrement',
};
