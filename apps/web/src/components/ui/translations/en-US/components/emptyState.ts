import type {Dictionary} from '../..';
import type {EmptyStateTranslations} from '../../../components/EmptyState/translations';

export const emptyState: Dictionary<EmptyStateTranslations> = {
    title: 'No items registered',
};
