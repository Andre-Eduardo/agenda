import type {Dictionary} from '../..';
import type {TagTranslations} from '../../../components/Tag/translations';

export const tag: Dictionary<TagTranslations> = {
    tagClose: {
        close: 'Close',
    },
};
