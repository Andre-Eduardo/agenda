import type {Dictionary} from '../..';
import type {AvatarTranslations} from '../../../components/Avatar/translations';

export const avatar: Dictionary<AvatarTranslations> = {
    user: 'usuário',
};
