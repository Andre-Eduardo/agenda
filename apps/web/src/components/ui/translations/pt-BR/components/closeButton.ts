import type {Dictionary} from '../..';
import type {CloseButtonTranslations} from '../../../components/CloseButton/translations';

export const closeButton: Dictionary<CloseButtonTranslations> = {
    close: 'Fechar',
};
